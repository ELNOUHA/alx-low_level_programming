#include "main.h"

/**
 * main - Entry point
 *
 * Descrption: print the alphabet, in lowercase
 *
 * Return: Always 0 (Success)
*/
int void print_alphabet(void);
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);
	_putchar('\n')

		return (0);
}
