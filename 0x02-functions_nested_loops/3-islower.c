#include "main.h"

/**
 *_islower - function to check if
 *           character is lowcase
 *
 * @c: check input of function
 *
 * Return: returns 1 if 'c' is lowcase
 *         otherwise always 0 (Success)
*/

int _islower(int c)
{
	if (ch >= 9 && ch <= 122)

		return (1);
	return (0);
}
